# coding: utf-8

"""
    PMXT Sidecar API

    A unified local sidecar API for prediction markets (Polymarket, Kalshi, Limitless). This API acts as a JSON-RPC-style gateway. Each endpoint corresponds to a specific method on the generic exchange implementation.

    The version of the OpenAPI document: 0.4.4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class EventFetchParams(BaseModel):
    """
    EventFetchParams
    """ # noqa: E501
    query: Optional[StrictStr] = None
    sort: Optional[StrictStr] = None
    limit: Optional[StrictInt] = 10000
    offset: Optional[StrictInt] = None
    status: Optional[StrictStr] = Field(default=None, description="Filter by event status (default: active)")
    search_in: Optional[StrictStr] = Field(default=None, alias="searchIn")
    event_id: Optional[StrictStr] = Field(default=None, description="Direct lookup by event ID", alias="eventId")
    slug: Optional[StrictStr] = Field(default=None, description="Lookup by event slug")
    __properties: ClassVar[List[str]] = ["query", "sort", "limit", "offset", "status", "searchIn", "eventId", "slug"]

    @field_validator('sort')
    def sort_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['volume', 'liquidity', 'newest']):
            raise ValueError("must be one of enum values ('volume', 'liquidity', 'newest')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['active', 'closed', 'all']):
            raise ValueError("must be one of enum values ('active', 'closed', 'all')")
        return value

    @field_validator('search_in')
    def search_in_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['title', 'description', 'both']):
            raise ValueError("must be one of enum values ('title', 'description', 'both')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EventFetchParams from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EventFetchParams from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "query": obj.get("query"),
            "sort": obj.get("sort"),
            "limit": obj.get("limit") if obj.get("limit") is not None else 10000,
            "offset": obj.get("offset"),
            "status": obj.get("status"),
            "searchIn": obj.get("searchIn"),
            "eventId": obj.get("eventId"),
            "slug": obj.get("slug")
        })
        return _obj


