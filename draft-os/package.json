{
  "name": "draft-os",
  "version": "1.0.0",
  "private": true,
  "description": "4th & Optimized Draft OS - local, offline-first fantasy football draft cockpit",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node build.js && node --test",
    "e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
