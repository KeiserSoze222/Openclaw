{
  "name": "draft-os-espn",
  "version": "1.0.0",
  "private": true,
  "description": "Draft OS - ESPN 12-team 1QB league (storage-isolated clone of draft-os)",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node build.js && node --test",
    "e2e": "npx playwright test",
    "serve": "node build.js && node serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
